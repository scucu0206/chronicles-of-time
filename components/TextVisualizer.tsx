
import React from 'react';

// This component has been replaced by TextParticleSystem.tsx inside the 3D Canvas
// Keeping file structure valid but empty to prevent import errors during transition
export const TextVisualizer: React.FC<any> = () => {
  return null;
};
